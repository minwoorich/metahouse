<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.multi.metahaus.Dashboard">
    <select id="getAllUsers" resultType="com.multi.metahouse.domain.dto.user.UserDTO">
        SELECT user_id, email, user_name, phone_number, gender, birth, create_date, user_grade, point
        FROM users
    </select>

</mapper>